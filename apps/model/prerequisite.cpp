#include "prerequisite.h"
#include <iostream>
#include <string>

Prerequisite::Prerequisite(std::string name, int id)
:name(name),id(id) {};

// std::string Recipe::getName() {
// 	return this->name;
// }